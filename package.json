{
  "name": "image-free-convert",
  "version": "1.0.0",
  "private": true,
  "description": "Lightweight web app for image format conversion, crop, and resize.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=12"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
